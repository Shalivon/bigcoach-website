# CLAUDE.md — הנחיות עבודה לפרויקט ביג קואוצ׳

קובץ זה נקרא אוטומטית על ידי Claude Code. הוא מגדיר את כל הקונבנציות, החוקים וההחלטות של הפרויקט. **לקרוא לפני כל שינוי.**

---

## מה זה הפרויקט

אתר תדמית וקונברסיה ל**ביג קואוצ׳** — מותג ליווי כושר, תזונה ואימון אישי של גולן בובליל (הנגב / דרום).
**אתר עצמאי (standalone), לא Webflow** — החלטה מ-2026-07. הפרונטאנד הוא single-file: כל ה-HTML, CSS ו-JavaScript בתוך `site/index.html`. מתארח ב-Vercel, עם שכבת בק-אנד קלה (`site/api/`, Vercel Serverless Functions) לטיפול בטפסים וחיבור עתידי ל-CRM ולוואטסאפ.

---

## חוקי עבודה (חשוב!)

1. **כל הקוד בקובץ אחד** — `site/index.html`. CSS בתוך `<style>`, JS בתוך `<script>` יחיד. לא לפצל לקבצים נפרדים אלא אם מתבקש מפורשות.
2. **אחרי כל עריכה — אימות חובה:**
   - איזון תגיות: `grep -c '<div' site/index.html` חייב להיות שווה ל-`grep -c '</div>'`.
   - תקינות JS: לחלץ את בלוק ה-`<script>` ולהריץ `node --check`.
   - **אל תדלג על האימות הזה.** הוא מנע באגים לאורך כל הפיתוח.
3. **RTL עברית** — האתר כולו ימין-לשמאל. כל פריסה חדשה חייבת לעבוד ב-RTL.
4. **עבודה איטרטיבית** — סקשן-סקשן, פידבק-תיקון. לא לכתוב מחדש בלוקים גדולים בלי בקשה.
5. **בלי המצאות** — לא להמציא פרטים על גולן, לא להמציא עדויות, מחירים או נתונים. אם חסר מידע — לשאול.

---

## זהות ויזואלית

### צבעים
| משתנה | ערך | שימוש |
|---|---|---|
| `--black` | `#232323` | רקע ראשי |
| `--white` | `#EEEEEE` | טקסט ראשי |
| `--red` | `#CA062C` | הדגשות, CTA |
| `--ink` | רקע כהה לכרטיסים | |
| `--card` | רקע כרטיס | |

טון: לוחמני, פוטוריסטי, נקי ומינימלי. שחור-לבן-אדום בלבד.

### פונטים (Google Fonts) — הוחלפו 2026-07 לבקשת הלקוח
- **כותרות** (`h1,h2,h3` וכו׳): `Rubik` — `var(--font-head)`, משקלים 700/800/900 (h1 = 900). חזק, ספורטיבי וקריא.
- **כל שאר הטקסט**: `Heebo` — `var(--font-run)`, משקלים 400–900. זה גם ה-body default.
- **חשוב**: Rubik רחב בהרבה מ-Karantina הישן — כל הגדלים כוילו מחדש (h1 עד 5.6rem, h2 עד 3.5rem). אם מגדילים כותרת, לבדוק גלישת שורות.
- גדלי טקסט רץ הוגדלו לסריקה מהירה (פסקאות 1.05–1.15rem).
- רשימת הסלקטורים שמקבלים פונט כותרת:
  `h1,h2,h3,.prog-title,.menu-link,.golan-card h3,.pd-head h3`

### טיפוגרפיה וטקסט
- **אסור מקפים בטקסט** — רק נקודות ופסיקים.
- placeholder בטקסט: סוגריים מרובעים `[כך]`.
- קול הקופי של גולן: שורות קצרות מקוטעות, טון אישי לא היררכי, הומור יבש, CTA רך, שימוש חוזר במילה "גאווה".

### אייקונים
כל החצים אחידים: SVG path `M14 8H2M2 8L7 3M2 8L7 13`, stroke-width `1.7`, round.

---

## מבנה הדף (`index.html`)

```
navbar (לוגו + כפתור שיחת היכרות)
└─ hero (#hero)              — כולל golan-cut.png ממורכז בחלל שבין הכותרת לקצה השמאלי (left מחושב: מרכז החלל הפנוי פחות חצי רוחב, דסקטופ בלבד)
└─ story (#story)            — נרטיב גלילה: שורות עולות בזו אחר זו; שורת 'יום ראשון' בנויה מפרגמנטים שדוחפים זה את זה ימינה (sticky + spacer 300vh, 8 צעדים)
└─ reels (#reels)            — קרוסלת סרטונים אנכיים 1080×1920: סיבוב אוטומטי + גרירה עם אינרציה (2 קבוצות משוכפלות ללולאה), fade בקצוות, סמן גרירה אדום בהובר
└─ strip (.strip)            — פס מותג נע: "זמינות מלאה 24/7 ✦ 100% תוצאות ✦ ליווי בכל הארץ ✦ קשר ישיר עם גולן ✦ בלי בולשיט ✦ תכניות מותאמות אישית"
└─ about (#about)            — "אני הבן אדם שיהיה שם כשקשה"
└─ golan (#golan)            — סיפור אישי, הסמכות
└─ method (#method)          — השיטה (לפני המסלולים בכוונה: מדריך → תוכנית → הצעה)
└─ programs (#programs)      — חוויית גלילה sticky בדסקטופ / כרטיסים במובייל
└─ testimonials (#testimonials)
└─ faq (#faq)
└─ final (#final) + footer
```
**מסע הלקוח (2026-07, לא לערבב סדר בלי בקשה)**: כאב (hero) → הזדהות (story) → הוכחה (reels) → המדריך (about+golan) → התוכנית (method) → ההצעה (programs) → הוכחה חברתית → התנגדויות → סגירה.
פופאפ לידים (`#leadPopup`) ותפריט נפתח (`#menuOverlay`) יושבים בראש ה-body. פופאפ ההנחה לכוחות הביטחון (`#discPopup`) **הוסר לצמיתות לבקשת הלקוח (2026-07) — לא להחזיר**.

---

## אחסון ובק-אנד (2026-07)

**החלטה**: לא Webflow. אתר עצמאי, מתארח ב-**Vercel**, נפרס אוטומטית מה-branch `main` בגיטהאב.

- **פרונטאנד**: `site/index.html` — נשאר vanilla single-file, בלי שינוי.
- **בק-אנד**: `site/api/lead.js` — Vercel Serverless Function (Node, בלי תלויות) שמקבלת POST מטופס הלידים (`#leadForm`). כרגע רק רושמת את הליד ל-logs של Vercel.
- **חיבור ל-CRM**: עדיין לא הוחלט אילו CRM. כדי לחבר, מגדירים משתני סביבה בפרויקט Vercel (Settings → Environment Variables), בלי לגעת בקוד:
  - `GOOGLE_SHEETS_WEBHOOK_URL` — URL של Google Apps Script Web App, אם רוצים גיליון כ-CRM זמני.
  - `CRM_WEBHOOK_URL` — כתובת webhook נכנס של כל CRM (HubSpot, GoHighLevel וכו׳).
- **וואטסאפ אוטומציה**: הוחלט על WhatsApp Business API דרך ספק (Twilio / 360dialog / Wati) — עדיין לא מחובר, דורש חשבון עסקי מאומת ופרטי גישה מהלקוח. עד אז נשארים על קישורי `wa.me` עם `?text=` מוכן (ראו "קישורים / placeholder למילוי").
- **טפסים בפרונטאנד**: מקום אחד שולח `fetch('/api/lead', …)` — `leadForm` (טופס הלידים המלא). לא לחזור ל-fetch ישיר מהדפדפן ל-Google Sheets (`mode:'no-cors'`) — זה נמנע מכוונה כי אי אפשר לקרוא את התשובה ולוודא הצלחה.

---

## רספונסיביות / מובייל

- Breakpoint ראשי: `@media (max-width:860px)`.
- **שים לב לספציפיות CSS** — הרבה באגים נבעו מכך שכלל בסיס (`nav:not(.menu-overlay)`) גבר על כלל מובייל (`nav`). כלל מובייל חייב להיות בספציפיות שווה או גבוהה יותר, או להופיע אחרי הבסיס.
- **viewport** — תג `<meta name="viewport" content="width=device-width, initial-scale=1.0">` חייב להישאר. בלעדיו מובייל מציג פורמט דסקטופ מכווץ.

### מצב מובייל לפי סקשן
- **הירו**: כפתור הצטרפות נעלם. תפריט ☰ עובר לפינה שמאל-עליונה (absolute, נגלל עם הדף, מיושר ללוגו, 56px). badge + sub + tagline מוסתרים. כותרת ענקית `clamp(3.6rem,13vw,5.5rem)`. CTA = כפתור וואטסאפ ירוק `#25D366` עם טקסט "לפרטים".
- **About**: תוכן ממורכז. כותרת רוחב מלא, "כשקשה" לבן + קו תחתון משיכת מכחול אדום. סטטים בטור שורה-אחת ("1,400+ סיימו" וכו׳, לבן + פלוסים אדומים). תמונה מוסתרת. כפתורים בטור ("מי אני?" גדול מ"צור קשר").
- **תוכניות**: כרטיסים מוערמים, סטאק ממורכז (כותרת + תיאור). לחיצה → תמונה מחשיכה, הסטאק נעלם, נפתח detail מצומצם (בלי מחירים/תדירות, בלי thumb, רק תיאור). `progScroll()` מקבל guard שמכבה את אנימציית הגלילה במובייל.
- **פאנלי התוכניות (2026-07)**: בלי מספור. `.prog-center` = סטאק ממורכז: כותרת ענקית (עד 5.2rem) → `.prog-cap` תיאור → `.prog-keep` השאר פרטים. כפתור הפלוס נשאר בפינה עם העיגול בלבד. במצב open הסטאק נעלם.

### מצב מובייל — סקשנים נוספים (הותאם בריבילד 2026-07)
- **golan**: טור אחד, מסגרת אדומה של התמונה מוסתרת, כרטיס padding מצומצם, CTA ממורכז.
- **method**: כרטיסים בטור אחד.
- **testimonials**: כרטיסים צרים יותר (300px / 240px), אותם סליידרים.
- **faq**: טקסט וריווח מוקטנים, מספור נשאר.
- **final/footer**: גריד בטור אחד ממורכז, socials ממורכזים.

### תוספות פרימיום (ריבילד 2026-07)
- שכבת grain עדינה (`.grain`, z-index 350), פס התקדמות גלילה אדום (`.progress`, מתעדכן ב-`progScroll`).
- מונים חיים בסטטים של about (`.cnum[data-count]`, IntersectionObserver).
- פרלקסה עדינה בהירו (`heroParallax`, כבוי במובייל וב-reduced-motion).
- מילת רקע ענקית ב-golan (`.golan-bigword`), מסגרת אדומה מוסטת לתמונה.
- **מלכודת ספציפיות שקרתה בפועל**: כלל בסיס בשני סלקטורים (0,2,0) גבר על כלל מובייל בסלקטור יחיד (0,1,0). כלל מובייל חייב ספציפיות שווה או גבוהה.
- **הוסרו לבקשת הלקוח (2026-07)**: eyebrows (כותרות קטנות), מספור סקשנים, `hero-foot`, `hero-tagline`. לא להחזיר.
- **פריסה**: רחבה, `--pad-x:clamp(1.4rem,4vw,3.5rem)`. ניסיון band ממורכז (17vw שוליים) בוטל לבקשת הלקוח 2026-07, לא להחזיר בלי בקשה מפורשת.
- **סרטונים**: `<div class="reel ph" data-video="vid-X.mp4">` + injector וידאו נפרד; קבצים ב-`assets/vid/`; play/pause לפי IntersectionObserver.
- **שכבת אינטראקציה (2026-07)**: סמן מותאם (נקודה + טבעת אדומה, רק hover+fine-pointer), כפתורים מגנטיים, tilt תלת-ממדי על mcard/reel/tcard, סקשן story מונע גלילה. הכל מכובה ב-reduced-motion ובמובייל/מגע. הסלקטורים המגנטיים: `.btn,.btn-join,.golan-lead-btn,.lead-submit`.
- **כוריאוגרפיית גלילה גורפת (2026-07)**: `.rv-h` (עלייה + blur) מוצמד אוטומטית ב-JS לכל h2 ב-main, sec-sub, פסקאות about/golan (עם stagger). `.rv-img` (חשיפת וילון clip-path) על תמונות about/golan — **נחשף דרך ה-parent** (`.reveal.in .rv-img`) כי Chrome מחזיר intersectionRatio 0 לאלמנט עם clip-path של 100%. לא לצפות ב-rv-img ישירות ב-IO.
- **אלמנטים צפים** (`.floater`, ✦ + טבעות): פרלקסה לפי `data-fspeed`, מוסתרים במובייל. הטיית מהירות (skew בגלילה) **בוטלה לבקשת הלקוח** — לא להחזיר.
- **שורות אישיות**: `.sec-sub` ב-method/testimonials/faq, `.final-kicker` לפני המרקיזה.

---

## מערכת התמונות (placeholder)

כל תמונה היא `<div class="ph" data-img="..." data-alt="..." data-label="...">`.
JS injector יוצר `<img>`; `onload` → מוסיף `has-img` (מסתיר את התווית המקווקוות); `onerror` → מסיר.
רשימת כל התמונות והמידות: `site/assets/img/README.md`.

---

## קישורים / placeholder למילוי

- וואטסאפ: `wa.me/972526896182`. **כל הקישורים כוללים `?text=` עם הודעה מוכנה** (כללית, או ספציפית למסלול בכפתורי התוכניות). לשמור על זה בשינויים.
- טלפון, מייל, handles, מחירים, עדויות — placeholder, למילוי לפי מידע אמיתי בלבד.
- לפני עלייה לאוויר: להחליף `[DOMAIN]` בתגיות ה-OG לדומיין האמיתי.

## החלטות המרה (2026-07, לא לשבור)

- **CTA אחיד בכל האתר**: "קבע שיחת היכרות" (מיקרו-קופי: "20 דקות, בלי עלות ובלי התחייבות"). לא להחזיר ניסוחים שונים.
- **פופאפ כוחות הביטחון (הנחה 15%)** — הוסר לצמיתות לבקשת הלקוח (2026-07). לא להחזיר.
- **כפתור וואטסאפ צף** (`.wa-fab`, ירוק, פינה שמאל-תחתונה) בדסקטופ ובמובייל.
- **מספר מתאמנים פעילים באתר: 250** (הירו + about). אם הלקוח מאשר מספר אחר, לעדכן בשני המקומות יחד.
- תמונות ההירו נטענות eager + fetchpriority=high, כל השאר lazy.

---

## תהליך אריזה לאחר שינוי

```bash
# (העריכה היא ישירות ב-site/index.html)
# אימות:
grep -c '<div' site/index.html ; grep -c '</div>' site/index.html   # חייבים להיות שווים
# חילוץ ובדיקת JS:
# (Claude Code יכול להריץ node --check על בלוק ה-script)
```

---

## דברים שאסור לעשות

- לא להמציא תוכן (סיפורים, עדויות, מחירים, נתונים על גולן).
- לא לפצל את index.html לקבצים בלי בקשה.
- לא לדלג על אימות div/JS.
- לא להוסיף dependencies / build tools / frameworks **לפרונטאנד**. `site/index.html` נשאר vanilla single-file. היוצא מן הכלל היחיד: `site/api/` (Vercel Serverless Functions, Node ללא תלויות) לבק-אנד.
- לא לשנות צבעים/פונטים מההגדרות למעלה בלי בקשה.
